/* const handleClick = async () => {
    upDateState({
      column: inputColumn,
      comparison: inputCompasion,
      value: inputValues,
    });
    // vou verificar se os planetas tem oque a pessoa selecionou e retornar colocando dentro do planetsFiltered
    const filter = await filterByNumber.forEach((element) => {
      console.log(element);
      if (element.comparison === 'maior que') {
        return Number(element[element.column] > Number(element.value));
      } if (element.comparison === 'menor que') {
        return Number(element[element.column] < Number(element.value));
      }
      return Number(element[element.column]) === Number(element.value);
    });
    console.log(filter);
    setPlanetsFiltered(filter);
  }; */
